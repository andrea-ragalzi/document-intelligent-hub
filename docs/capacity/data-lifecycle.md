# Public-demo data lifecycle

This document records the configured lifecycle controls for the public portfolio
demo. It does not assume storage-provider pricing, quota, TTL, or lifecycle
features that are not configured in this repository.

| Resource                               | Creation path                                                                                                                      | Configured maximum                                                                                                                                                                                                                                                   | Deletion / cleanup path                                                                                                                                      | Possible orphan scenario                                                                                                                                     | Test coverage                                                                            | Unresolved risk                                                                                                     |
| -------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| Uploaded original PDF                  | `POST /rag/upload/` validates the stream, then `DocumentFileStorage.store` writes the owner-scoped original.                       | Fallback FREE limits are 5 PDFs and 10 MB per PDF; higher configured tiers may differ. The bundled Alice PDF counts as one document.                                                                                                                                 | Single document delete removes the original after its chunks; bulk delete removes the user original directory.                                               | A cross-store delete failure can leave an original after vector deletion.                                                                                    | Document storage owner/delete/re-upload tests; router lifecycle tests.                   | Persistent-volume retention and provider lifecycle policy are **UNVERIFIED**.                                       |
| Parsed/extracted content               | `DocumentIndexingService` parses a temporary PDF with `UnstructuredPDFLoader`.                                                     | Constrained tiers allow at most 150 pages and 500,000 extracted characters per PDF; `UNLIMITED` bypasses these application limits.                                                                                                                                   | The temporary upload PDF is removed in `finally`; parsed content is only held in process memory.                                                             | Process interruption can bypass normal process cleanup.                                                                                                      | Valid and malformed-PDF lifecycle tests verify temporary-file cleanup.                   | Host temporary-directory cleanup after an abnormal process termination is **UNVERIFIED**.                           |
| Chunks and embeddings / vector entries | Parsed content is chunked, annotated with owner and filename, then written through `VectorStoreRepository`.                        | Constrained tiers allow at most 1,500 chunks per PDF. There is no independent per-user vector cap; tier file-count limits provide the aggregate bound. `UNLIMITED` bypasses the per-document chunk cap.                                                              | Single document deletion filters by verified owner and filename; bulk deletion filters by verified owner. Failed indexing compensates for any partial write. | Provider-side partial write or an interrupted compensation operation.                                                                                        | Repository owner/file and bulk-deletion tests; failed-ingestion cleanup tests.           | Vector-store retention, physical reclamation, and provider quota are **UNVERIFIED**.                                |
| Per-document vector metadata           | The indexer attaches owner ID, filename, language, timestamp, and an Alice provenance marker to starter chunks.                     | Bounded by the applicable tier's ingestion and file-count limits; `UNLIMITED` has no application-level document/chunk ceiling.                                                                                                                                       | Removed with the chunk filter during single and bulk document deletion.                                                                                      | Legacy chunks without expected metadata can evade a metadata-specific audit.                                                                                 | Repository deletion and multi-tenant isolation tests.                                    | Pre-existing production data has not been audited here.                                                             |
| Conversations and inline messages      | The browser saves a conversation through `saveConversationToFirestore`; local-storage migration uses the same save path.           | 20 conversations per user; 40 persisted messages per conversation; 12,000 persisted message-text characters per conversation. `frontend/lib/conversationsService.ts` trims to the most recent messages/text. Firestore rules also reject histories over 40 messages. | `deleteConversationFromFirestore` deletes the conversation document. Messages are inline `history`, so there is no message subcollection to orphan.          | A signed-in client can create conversation documents outside the service; Firestore rules cannot enforce a per-user document count or aggregate text length. | Conversation-service cap/migration tests and Firestore-rules boundary tests.             | Firestore TTL/retention policy is **UNVERIFIED**; retention relies on user deletion and the configured service cap. |
| User-specific Firestore metadata       | Query-use records are created by `FirestoreUsageTracker`; conversation ownership metadata is created with each conversation.       | Query records retain 30 days when `cleanup_old_usage` runs. Conversation metadata is bounded with its conversation.                                                                                                                                                  | Usage cleanup removes old daily entries; deleting a conversation removes its inline ownership metadata.                                                      | Usage cleanup is not invoked by a scheduler in this repository.                                                                                              | Usage cleanup implementation exists; its legacy cleanup tests require activation/repair. | A deployed scheduler and Firestore TTL configuration are **UNVERIFIED**.                                            |
| Temporary/intermediate files           | Upload bytes are copied to a `mkstemp` PDF for parsing.                                                                            | One active upload per UID in the demo process; file-size and extraction ceilings apply.                                                                                                                                                                              | Indexing always removes the temporary PDF in `finally`.                                                                                                      | Host crash before `finally`.                                                                                                                                 | Document lifecycle malformed-PDF test.                                                   | OS-level temp-file lifecycle after crash is **UNVERIFIED**.                                                         |
| Failed or partial ingestion jobs       | The upload router stores an original, then indexes it; indexing failures are surfaced as errors.                                   | Constrained tiers apply the configured file-size limit plus the 150-page, 500,000-character, and 1,500-chunk ceilings before vector growth; `UNLIMITED` bypasses those ingestion ceilings.                                                                           | Failed indexing removes the just-stored original and compensates any indexed chunks for that owner/filename.                                                 | A process/provider failure during compensation can leave a partial resource.                                                                                 | Failed-ingestion and cleanup tests.                                                      | No durable job queue or external reconciliation worker is configured.                                               |
| Registered Alice starter              | `POST /rag/documents/seed-demo` creates the existing private Alice starter PDF/vector set for registered onboarding only.          | One starter document per registered user; it counts toward the five-PDF allowance.                                                                                                                                            | Normal registered-user document deletion paths remove the starter.                                                                                           | Account deletion performed only in the client can fail partway through cleanup.                                                                              | Existing seed idempotency and deletion tests.                                               | This path is not used by anonymous guests.                                                                                |
| Shared InGen guest dataset            | Backend startup provisions the five bundled synthetic PDFs in `__shared_ingen_demo_v1__` only when that namespace is absent or incomplete. | Exactly five stable documents shared by all anonymous sessions; guests cannot invoke ingestion or mutation endpoints.                                                                                                         | Operators can disable provisioning or deliberately clear the reserved namespace and originals; startup repairs an incomplete namespace.                    | A process failure during first provisioning can leave partial data, which the service compensates and retries on next startup.                               | Shared-corpus idempotency, stable metadata, guest namespace, and mutation-boundary tests. | The deployed persistent volume must retain both Chroma and original PDF paths across restarts.                            |
| Guest query counters                  | A pre-RAG reservation transaction updates UID, hashed-IP, and global counters in `guest_demo_usage/current`.                       | Defaults: 8 per UID/day, 20 per IP/day, and 100 total/day. One rolling document bounds persistent counter documents and resets its maps on UTC date change.                                                                    | The next UTC day's first reservation replaces the prior day's maps in the same document.                                                                      | An unavailable Firestore transaction fails closed and rejects the guest query.                                                                                | Service boundary and direct HTTP rejection tests.                                           | App Check is not enforced; UID recreation is contained by IP and global ceilings rather than prevented.                  |

## Configured limits

- Default FREE uploads: 5 PDFs per user, 10 MB per PDF.
- Constrained-tier parsing: 150 pages and 500,000 extracted characters per PDF.
- Constrained-tier chunks and embeddings: 1,500 per PDF; aggregate growth is bounded by the user's tier file-count limit rather than a separate vector cap.
- Persisted conversations: 20 per user, 40 messages and 12,000 message-text characters per conversation.
- Guest RAG defaults: 8 queries per anonymous UID/day, 20 per hashed IP/day, 100 globally/day, and 1 concurrent guest query per backend process.

The ingestion limits are configured in the backend tier/indexing constants. The
conversation limits are enforced in `frontend/lib/conversationsService.ts`, and
the 40-message structural ceiling is duplicated in `frontend/firestore.rules`.

## Public-demo data lifecycle status

**PUBLIC-DEMO DATA LIFECYCLE: IMPLEMENTED; DEPLOYMENT VERIFICATION REQUIRED**

Remaining blockers:

- The deployed persistent volume, Firebase Anonymous provider, production Firestore rules, proxy IP configuration, and provider quotas remain to be verified against the live environment.
- App Check is not enforced. The current controls bound paid guest queries even when anonymous identities are recreated.
