# MCP SonarQube Configuration
# Copy this to your shell profile (~/.bashrc or ~/.zshrc)

# SonarQube Cloud Token
# Get your token from: https://sonarcloud.io/account/security
export SONARQUBE_TOKEN="your-token-here"

# Organization (already configured in mcp.json)
export SONARQUBE_ORG="andrea-ragalzi"

# IDE Port (already configured in mcp.json)
export SONARQUBE_IDE_PORT="64120"
