# === OpenAI API Configuration ===
# Required for LLM operations (RAG queries, summarization, etc.)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# === SendGrid Configuration (Bug Reports) ===
# Required for email notifications when users report bugs
# Setup instructions:
# 1. Sign up at https://sendgrid.com (100 emails/day free)
# 2. Go to Settings -> API Keys -> Create API Key
# 3. Give it "Mail Send" permission
# 4. Verify your sender email in SendGrid dashboard
SENDGRID_API_KEY=SG.your-api-key-here
SENDGRID_FROM_EMAIL=noreply@yourdomain.com
SUPPORT_EMAIL=support@yourdomain.com

# === ChromaDB Configuration ===
# Path to ChromaDB persistent storage
CHROMA_DB_PATH=chroma_db

# === LLM Model Selection ===
# Default: gpt-3.5-turbo (cheaper, faster)
# Alternative: gpt-4 (more accurate, more expensive)
LLM_MODEL=gpt-3.5-turbo

# === Optional: Cohere for Reranking ===
# Improves retrieval accuracy but adds cost
# COHERE_API_KEY=your-cohere-key-here
